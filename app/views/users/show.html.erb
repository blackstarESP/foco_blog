<h1 class="text-center">Welcome to <%= @user.username %>'s page</h1>
<div class="row">
  <div class="col-md-4 col-md-offset-4 center">
    <%= gravitar_for @user, size: 150 %>
  </div>
</div>
<h4 class="text-center"><%= @user.username %>'s articles</h4>

<!-- Article pagination above article content -->
<div class="text-center"><%= will_paginate @user_articles %></div>

<!-- Article content loop -->
<%= render 'articles/article', obj: @user_articles %>

<!-- Article pagination below article content -->
<div class="text-center"><%= will_paginate @user_articles %></div>
